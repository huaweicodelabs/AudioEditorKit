<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.huawei.hms.audioeditor.demo">

   <!-- TODO step 1 :Add necessary permissions in the AndroidManifest.xml file.-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.VideoEditDemo">
        <!-- TODO step 2.Create your app's custom activity for selecting one or more audios. When you choose to import the audios, the SDK will send an intent whose action value is com.huawei.hms.audioeditor.chooseaudio to jump to the activity. Register this activity in the AndroidManifest.xml-->
          <activity android:name=".SettingActivity"></activity>
          <activity android:name=".MainActivity">
              <intent-filter>
                  <action android:name="android.intent.action.MAIN" />

                  <category android:name="android.intent.category.LAUNCHER" />
              </intent-filter>
          </activity>
          <activity android:name=".AudioFormatActivity" />

      </application>

  </manifest>